{
  "name": "theswarm",
  "version": "1.0.0",
  "description": "The Swarm - AI agents coordinating at scale",
  "private": true,
  "packageManager": "npm@10.0.0",
  "scripts": {
    "dev": "cd packages/dashboard && npm run dev",
    "build": "cd packages/dashboard && npm run build",
    "test": "cd packages/dashboard && npm run test",
    "lint": "cd packages/dashboard && npm run lint",
    "clean": "rm -rf packages/*/node_modules packages/*/.next .turbo node_modules"
  },
  "devDependencies": {
  }
}
